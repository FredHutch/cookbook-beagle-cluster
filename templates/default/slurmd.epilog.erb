#!/bin/bash

SCRATCH_ROOT=/mnt/<%= @config['ClusterName'] %>/scratch

if [ $SLURM_JOB_ID -gt 0 ] && [ -d $SCRATCH_ROOT/$SLURM_JOB_ID ]
then
	/bin/rm -rf $SCRATCH_ROOT/$SLURM_JOB_ID
  /bin/rm -rf /loc/scratch/${SLURM_JOB_ID}
fi

echo "removed scratch directories"

exit 0
